{
  "name": "s-img",
  "version": "0.0.0",
  "description": "Pure TypeScript image manipulation for Node and Bun. No native binaries, no compile step.",
  "type": "module",
  "license": "MIT",
  "author": "Abdulkader Safi",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "build": "tsc",
    "test": "node --test 'test/**/*.test.ts'",
    "test:bun": "bun run test/smoke.bun.ts",
    "check:guards": "node scripts/guards.mjs",
    "check:size": "node scripts/size.mjs",
    "check": "tsc -p tsconfig.test.json && node scripts/guards.mjs && node --test 'test/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
